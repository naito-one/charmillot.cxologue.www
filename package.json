{
  "name": "charmillot.cxologue.www",
  "version": "1.0.0",
  "description": "Cxologue.ch Website",
  "author": "Tim Winkelmann",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_OPTIONS=--openssl-legacy-provider NUXT_HOST=0.0.0.0 nuxt",
    "build": "nuxt build",
    "start": "cross-env NODE_OPTIONS=--openssl-legacy-provider NUXT_HOST=0.0.0.0 nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "nuxt": "^2.15.8"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "lodash": "^4.17.21",
    "node-sass": "^6.0.0",
    "nuxt-purgecss": "^1.0.0",
    "prettier": "2.8.8",
    "push-dir": "^0.4.1",
    "sass-loader": "^10.1.1",
    "tailwindcss": "^1.8.13"
  }
}
